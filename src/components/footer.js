import * as React from "react"

const Footer = () => {
    return (
        <footer>
            <div>Footer Text { (new Date()).getFullYear() }</div>
        </footer>
    )
}

export default Footer
